class Solution:
    def searchMatrix(self, matrix: List[List[int]], target: int) -> bool:
        #also can be done in binary search
        for row in matrix:
            if target in row:
                return True
        return False
