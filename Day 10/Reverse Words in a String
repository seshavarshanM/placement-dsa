class Solution:
    def reverseWords(self, s: str) -> str:
        words =[]
        word = ""
        for i in s:
            if i != " ":
                word+=i
            else:
                if word != "":
                    words.append(word)
                    word=""
        if word:
            words.append(word)
        word = ""
        for i in range(len(words)-1,-1,-1):
            word+= words[i]
            if i!=0:
                word+=" "
        return word
